# =========================================
# POSTGRESQL DATABASE CONNECTION
# =========================================
# Rimuovi il commento e compila queste variabili per usare PostgreSQL.
# Lascia commentato per usare il fallback su SQLite.
#
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=mysecretpassword
# DB_NAME=real_estate_crm
#
# La connection string verrà costruita automaticamente.
# DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"