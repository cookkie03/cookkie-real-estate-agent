# ==============================================================================
# CRM IMMOBILIARE - PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file as `.env` or `.env.production` in the root directory
# 2. Fill in all required values with your production credentials
# 3. NEVER commit the .env file to Git
# 4. Keep this file secure and backed up separately
#
# ==============================================================================

# ------------------------------------------------------------------------------
# DATABASE - PostgreSQL
# ------------------------------------------------------------------------------
# Production PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://crm_user:CHANGE_THIS_PASSWORD@database:5432/crm_immobiliare"

# Database credentials (for docker-compose)
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=crm_immobiliare

# ------------------------------------------------------------------------------
# APPLICATION
# ------------------------------------------------------------------------------
# Environment mode (always production for deployment)
NODE_ENV=production

# Application URL (your production domain)
NEXTAUTH_URL=https://your-domain.com

# Session secret (generate with: openssl rand -base64 32)
SESSION_SECRET=GENERATE_A_STRONG_RANDOM_STRING_HERE

# NextAuth secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=GENERATE_A_DIFFERENT_STRONG_RANDOM_STRING_HERE

# Application port (default: 3000, usually don't change)
PORT=3000

# ------------------------------------------------------------------------------
# GOOGLE AI API (GEMINI)
# ------------------------------------------------------------------------------
# Required for AI-powered search and features
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# ------------------------------------------------------------------------------
# OPENROUTER API
# ------------------------------------------------------------------------------
# Required for: RAG Assistant, Matching AI, Daily Briefing
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# ------------------------------------------------------------------------------
# GITHUB CONTAINER REGISTRY
# ------------------------------------------------------------------------------
# GitHub username/org for pulling Docker images (for Watchtower)
GITHUB_REPOSITORY_OWNER=cookkie03

# ------------------------------------------------------------------------------
# WATCHTOWER (OPTIONAL)
# ------------------------------------------------------------------------------
# Notification settings for Watchtower updates
# Uncomment and configure if you want update notifications

# Slack webhook for notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email notifications (requires SMTP)
# WATCHTOWER_NOTIFICATION_EMAIL_FROM=watchtower@your-domain.com
# WATCHTOWER_NOTIFICATION_EMAIL_TO=admin@your-domain.com
# WATCHTOWER_NOTIFICATION_EMAIL_SERVER=smtp.gmail.com
# WATCHTOWER_NOTIFICATION_EMAIL_SERVER_PORT=587
# WATCHTOWER_NOTIFICATION_EMAIL_SERVER_USER=your-email@gmail.com
# WATCHTOWER_NOTIFICATION_EMAIL_SERVER_PASSWORD=your-app-password

# ------------------------------------------------------------------------------
# WEB SCRAPING - PORTALI IMMOBILIARI (OPTIONAL)
# ------------------------------------------------------------------------------
# Credentials for automatic scraping from real estate portals

# Immobiliare.it
# IMMOBILIARE_IT_USERNAME=your_username
# IMMOBILIARE_IT_PASSWORD=your_password

# Idealista.it
# IDEALISTA_IT_USERNAME=your_username
# IDEALISTA_IT_PASSWORD=your_password

# Casa.it
# CASA_IT_USERNAME=your_username
# CASA_IT_PASSWORD=your_password

# ------------------------------------------------------------------------------
# MAP API (OPTIONAL)
# ------------------------------------------------------------------------------
# By default uses OpenStreetMap (free)
# For advanced maps, get Mapbox key at: https://www.mapbox.com/
# MAPBOX_API_KEY=pk.eyJ1...your-mapbox-key

# ------------------------------------------------------------------------------
# EMAIL (OPTIONAL - FUTURE FEATURE)
# ------------------------------------------------------------------------------
# For automated emails (follow-ups, notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@your-domain.com

# ------------------------------------------------------------------------------
# EMBEDDINGS API (OPTIONAL)
# ------------------------------------------------------------------------------
# For RAG system embeddings (if using external service)
# EMBEDDING_API_KEY=your-embedding-api-key
# EMBEDDING_PROVIDER=openai  # openai, cohere, local

# ------------------------------------------------------------------------------
# MONITORING & LOGGING (OPTIONAL)
# ------------------------------------------------------------------------------
# Sentry for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project

# LogRocket for session replay
# LOGROCKET_APP_ID=your-logrocket-app-id

# ==============================================================================
# SECURITY CHECKLIST BEFORE DEPLOYMENT:
# ==============================================================================
# [ ] Changed all default passwords
# [ ] Generated strong random secrets (SESSION_SECRET, NEXTAUTH_SECRET)
# [ ] Configured NEXTAUTH_URL with your production domain
# [ ] Added GOOGLE_API_KEY and OPENROUTER_API_KEY
# [ ] Verified DATABASE_URL is correct
# [ ] This file is added to .gitignore
# [ ] Backups are configured for database and uploads
# [ ] SSL/TLS is configured for production domain
# [ ] Firewall rules allow only necessary ports (80, 443, optionally 3000)
# ==============================================================================
