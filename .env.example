# ==============================================================================
# CRM IMMOBILIARE - CONFIGURAZIONE
# ==============================================================================
#
# SETUP RAPIDO:
# 1. Copia questo file come .env:
#    cp .env.example .env
#
# 2. Modifica i valori contrassegnati con ⚠️ OBBLIGATORIO
#
# 3. Genera i secrets:
#    openssl rand -base64 32
#
# 4. Avvia Docker:
#    docker-compose up -d
#
# ==============================================================================

# ------------------------------------------------------------------------------
# ⚠️ DATABASE (OBBLIGATORIO PER PRODUZIONE)
# ------------------------------------------------------------------------------
# User e nome database (puoi lasciare i default)
POSTGRES_USER=crm_user
POSTGRES_DB=crm_immobiliare

# ⚠️ Password database - CAMBIA QUESTO IN PRODUZIONE!
POSTGRES_PASSWORD=crm_password_CHANGE_THIS

# Porta database (default: 5432, modificare solo se in conflitto)
POSTGRES_PORT=5432

# ------------------------------------------------------------------------------
# ⚠️ AUTHENTICATION (OBBLIGATORIO)
# ------------------------------------------------------------------------------
# ⚠️ Session secret - Genera con: openssl rand -base64 32
SESSION_SECRET=GENERATE_WITH_openssl_rand_base64_32

# ⚠️ NextAuth secret - Genera con: openssl rand -base64 32
NEXTAUTH_SECRET=GENERATE_WITH_openssl_rand_base64_32

# ⚠️ URL applicazione
# Development: http://localhost:3000
# Production: https://tuo-dominio.com
NEXTAUTH_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# ⚠️ API KEYS (OBBLIGATORIO PER FEATURES AI)
# ------------------------------------------------------------------------------
# ⚠️ Google AI (Gemini) - Richiesto per ricerca AI
# Ottieni la chiave su: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# ⚠️ OpenRouter - Richiesto per RAG Assistant, Matching, Briefing
# Ottieni la chiave su: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# ------------------------------------------------------------------------------
# APPLICATION
# ------------------------------------------------------------------------------
# Ambiente (development o production)
NODE_ENV=production

# Porte applicazione (modificare solo se in conflitto)
APP_PORT=3000
AI_PORT=8000

# ------------------------------------------------------------------------------
# GITHUB (PER AUTO-UPDATE)
# ------------------------------------------------------------------------------
# Username GitHub (per scaricare immagini Docker da GHCR)
GITHUB_REPOSITORY_OWNER=cookkie03

# ------------------------------------------------------------------------------
# WATCHTOWER (OPZIONALE)
# ------------------------------------------------------------------------------
# Frequenza controllo aggiornamenti in secondi (default: 300 = 5 minuti)
WATCHTOWER_POLL_INTERVAL=300

# Debug mode per vedere log dettagliati Watchtower (true/false)
WATCHTOWER_DEBUG=false

# Notifiche Slack (opzionale)
# WATCHTOWER_NOTIFICATIONS=slack
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ------------------------------------------------------------------------------
# SCRAPING (OPZIONALE - FEATURE FUTURA)
# ------------------------------------------------------------------------------
# Credenziali portali immobiliari (non ancora implementato)
# IMMOBILIARE_IT_USERNAME=
# IMMOBILIARE_IT_PASSWORD=
# IDEALISTA_IT_USERNAME=
# IDEALISTA_IT_PASSWORD=
# CASA_IT_USERNAME=
# CASA_IT_PASSWORD=

# ------------------------------------------------------------------------------
# MAPS (OPZIONALE)
# ------------------------------------------------------------------------------
# Di default usa OpenStreetMap (gratuito)
# Per mappe avanzate usa Mapbox: https://www.mapbox.com/
# MAPBOX_API_KEY=

# ------------------------------------------------------------------------------
# EMAIL (OPZIONALE - FEATURE FUTURA)
# ------------------------------------------------------------------------------
# SMTP per invio email automatiche (non ancora implementato)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=

# ==============================================================================
# ✅ CHECKLIST PRIMA DI AVVIARE:
# ==============================================================================
# [ ] POSTGRES_PASSWORD modificata (non usare default in produzione!)
# [ ] SESSION_SECRET generato (openssl rand -base64 32)
# [ ] NEXTAUTH_SECRET generato (openssl rand -base64 32)
# [ ] NEXTAUTH_URL configurato con il tuo dominio
# [ ] GOOGLE_API_KEY aggiunto
# [ ] OPENROUTER_API_KEY aggiunto
# [ ] Questo file salvato come .env (NON committare su Git!)
# ==============================================================================
