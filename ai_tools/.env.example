# ==============================================================================
# Python AI Backend - Environment Variables Template
# ==============================================================================
#
# ISTRUZIONI:
# 1. Copia questo file come `.env` nella directory python_ai/
# 2. Compila i valori con le tue credenziali reali
# 3. NON committare MAI il file .env su Git
#
# ==============================================================================

# ------------------------------------------------------------------------------
# DATABASE
# ------------------------------------------------------------------------------
# Path relativo al database SQLite condiviso con Prisma
DATABASE_URL=sqlite:///../prisma/dev.db

# ------------------------------------------------------------------------------
# GOOGLE AI (GEMINI)
# ------------------------------------------------------------------------------
# Necessario per tutti gli agenti AI
# Ottieni la tua chiave su: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_ai_api_key_here

# Modello di default
GOOGLE_MODEL=gemini-1.5-pro

# ------------------------------------------------------------------------------
# QDRANT VECTOR STORE
# ------------------------------------------------------------------------------
# Per RAG e ricerca semantica
# Opzione 1: In-memory (per sviluppo)
QDRANT_MODE=memory

# Opzione 2: Docker locale (per produzione)
# QDRANT_MODE=server
# QDRANT_HOST=localhost
# QDRANT_PORT=6333

# ------------------------------------------------------------------------------
# CACHING
# ------------------------------------------------------------------------------
# Directory cache (git-ignored)
CACHE_DIR=.cache

# TTL cache in secondi (default: 1 ora)
CACHE_TTL=3600

# ------------------------------------------------------------------------------
# FASTAPI SERVER
# ------------------------------------------------------------------------------
# Host e porta server
HOST=127.0.0.1
PORT=8000

# CORS Origins (separati da virgola)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Ambiente
ENVIRONMENT=development

# ------------------------------------------------------------------------------
# LOGGING
# ------------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log format: json, console
LOG_FORMAT=console

# ------------------------------------------------------------------------------
# OPENTELEMETRY TRACING
# ------------------------------------------------------------------------------
# Abilitare tracing (true/false)
ENABLE_TRACING=true

# Service name per tracing
SERVICE_NAME=crm-immobiliare-ai

# ------------------------------------------------------------------------------
# AI AGENTS CONFIGURATION
# ------------------------------------------------------------------------------
# Temperature per generazione (0.0 - 1.0)
AI_TEMPERATURE=0.7

# Max tokens per risposta
AI_MAX_TOKENS=2048

# Timeout richieste AI (secondi)
AI_TIMEOUT=60

# ------------------------------------------------------------------------------
# RAG CONFIGURATION
# ------------------------------------------------------------------------------
# Numero documenti da recuperare
RAG_TOP_K=5

# Chunk size per document splitting
RAG_CHUNK_SIZE=512

# Chunk overlap
RAG_CHUNK_OVERLAP=50

# Embedding model
EMBEDDING_MODEL=models/text-embedding-004
